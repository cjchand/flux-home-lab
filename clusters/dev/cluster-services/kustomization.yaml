apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - external-mdns.yaml
  - nfs-external-provisioner-helmrelease.yaml
  - sealed-secrets-helmrelease.yaml
  - source-helmrepo-nfs-external-provisioner.yaml
  - source-helmrepo-sealed-secrets.yaml
  - cert-manager-namespace.yaml
  - cert-manager-helmrelease.yaml
  - source-helmrepo-cert-manager.yaml
  - namespace.yaml
  - helm-repository-cloudnative-pg.yaml
  - cloudnative-pg-operator-helmrelease.yaml
  # NOTE: internal-ca-issuer.yaml is excluded here because it requires
  # cert-manager CRDs to be installed first. Apply it manually after
  # cert-manager is ready, or add it back to this kustomization once
  # cert-manager installation is complete.
